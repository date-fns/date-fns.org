import { style } from '@vanilla-extract/css'

export const optionalLabel = style({
  fontStyle: 'italic',
})

export const propsLabel = style({
  fontWeight: 'bold',
  marginTop: '10px',
  marginBottom: '5px',
})
